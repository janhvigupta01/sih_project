version: '3.8'

services:
  scrapsathi-app:
    build: .
    container_name: scrapsathi_core
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
      - JWT_SECRET=scrap_sathi_sih_2026_secret_key_mo_mines
    restart: always
